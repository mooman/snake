let screenWidth = scene.screenWidth()
let screenHeight = scene.screenHeight()
let gridSize = 8
let snake = []
let snakeSprite = sprites.create(assets.image`cat`)
game.onUpdateInterval(500, function gameUpdate() {
    
})
